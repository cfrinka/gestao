[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]
cacheDirectories = []

[phases.build]
cmds = ["npm run build:demo"]
cacheDirectories = [".next/cache"]

[start]
cmd = "npm run start:demo"

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"
